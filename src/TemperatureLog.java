/*
 * Name:
 * Description:
 * Created by:
 * Last edited:
 */
import java.util.Scanner;

public class TemperatureLog {


    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

        // read n, then declare count, sum, min, and max for the two loops below
        int n = Integer.parseInt(input.nextLine());


        // read n readings with a for loop -- initialize min/max from the first reading, then update count, sum, min, max for the rest


        // read further readings with a while loop and a priming read, stopping once one equals -1000 (not counted) -- update the same count, sum, min, max


        // calculate average as sum / count


        // print the five output lines (README.md); Sum, Average, Minimum, Maximum via printf %.2f

    }
}
